ids of posts:

curl -X GET \
  'https://graph.instagram.com/me/media?fields=id,username&access_token={token}'


getting post by id:

curl -X GET \
    'https://graph.instagram.com/{id}?fields=id,media_type,media_url,username,timestamp&access_token={token}'